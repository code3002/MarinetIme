{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace packages/shared && npm run prisma:generate && npm run build --workspace apps/web && npm run build --workspace apps/api"
  },
  "deploy": {
    "startCommand": "node apps/api/dist/src/server.js",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
